import { VideoAgeBorderDirective } from "./video-age-border.directive";

describe("VideoAgeBorderDirective", () => {
    it("should create an instance", () => {
        const directive = new VideoAgeBorderDirective();
        expect(directive).toBeTruthy();
    });
});
